/**
 * Platzhalter für die von Supabase generierten Datenbank-Typen.
 *
 * Sobald dein Supabase-Projekt läuft und supabase/schema.sql eingespielt ist,
 * kannst du die echten Typen generieren (Supabase CLI nötig):
 *
 *   npx supabase gen types typescript --project-id DEIN-PROJEKT-ID > src/types/database.ts
 *
 * Bis dahin reicht dieser lockere Platzhalter, damit das Projekt kompiliert.
 */
export type Database = any;
